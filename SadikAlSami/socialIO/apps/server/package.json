{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.mjs"
  },
  "dependencies": {
    "@socialIO/auth": "workspace:*",
    "@socialIO/db": "workspace:*",
    "@socialIO/env": "workspace:*",
    "@hono/node-server": "^2.0.2",
    "@hono/zod-validator": "^0.8.0",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "^4.12.18",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@socialIO/config": "workspace:*",
    "@types/node": "^25.6.2",
    "tsdown": "^0.22.0",
    "tsx": "^4.21.0",
    "typescript": "catalog:"
  }
}
